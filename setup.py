# This file is only present for backwards compatibility with older pip versions.
# Modern pip and setuptools use pyproject.toml for configuration.
# Run: pip install -e . to install the package.

from setuptools import setup

setup()
